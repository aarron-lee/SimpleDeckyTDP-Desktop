# Build Electron app

```bash
# git clone repo

cd SimpleDeckyTDP-Desktop
npm install

npm run electron-build

cd ./electron
npm install

npm run build

# output will be in the electron/dist/SimpleDeckyTDP-Desktop.AppImage
```
